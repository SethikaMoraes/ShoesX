# Legacy Files

This folder contains archived files from the pre-React (plain HTML/CSS/JS) ShoesX version.

- `vanilla/app.js`
- `vanilla/style.css`
- `vanilla/js/*`
- `vanilla/assets/*`
- `vanilla/models/*`

These files are not used by the Vite React app in `src/` and `public/`.
